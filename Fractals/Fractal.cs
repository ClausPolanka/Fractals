namespace Fractals
{
    public class Fractal
    {
        private readonly int length;

        public Fractal(int length)
        {
            this.length = length;
        }

        public int Perimeter(int interation)
        {
            return -1;
        }
    }
}